apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: oci-coordinator
  namespace: oci-ai
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: oci-coordinator
