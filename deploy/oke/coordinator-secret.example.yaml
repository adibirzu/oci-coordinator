apiVersion: v1
kind: Secret
metadata:
  name: coordinator-secret
  namespace: oci-ai
type: Opaque
stringData:
  # Optional if gateway auth is enabled
  MCP_GATEWAY_BEARER_TOKEN: "replace-me"

  # Optional LLM provider credentials
  OPENAI_API_KEY: ""
  OCI_COMPARTMENT_OCID: ""
  OCI_GENAI_MODEL_ID: ""

  # Optional Redis auth
  REDIS_PASSWORD: ""
